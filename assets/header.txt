BONJOUR



    // int y = 0;
    // int x = 0;
    // initscr();
    // start_color();
    // init_pair(1, COLOR_CYAN, COLOR_BLACK);
    // while (1) {
    //     clear();
    //     getmaxyx(stdscr, y, x);
    //     x = x / 2 - my_strlen(av[1]) / 2;
    //     y = y / 2;
    //     attron(COLOR_PAIR(1));
    //     mvprintw(y, x, av[1]);
    //     attroff(COLOR_PAIR(1));
    //     refresh();
    //     if (getch() == ' ')
    //         break;
    // }
    // endwin();